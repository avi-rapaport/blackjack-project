services:
  app:
    build: ./
    container_name: blackjack
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    env_file:
      - .env

volumes:
  node_modules:
